#include "../../Include.js";var APPLICATION_ROOT = '/Users/calhoun/Work/HOAX-002';var renderer = new RendererPhotoshop();renderer.setConfig(new RendererPhotoshopConfig());renderer.init();var canvas = new Canvas();canvas.withConfirm = true;canvas.setRenderer(renderer);canvas.setDebugLevel(1);CSGlobal.registerCanvas(canvas);//canvas.statistics = new CSCanvasStatistics(canvas);canvas.setCanvasRoot(APPLICATION_ROOT);canvas.setName('hoax-001.psd');/*canvas.setCanvasOffset(0);canvas.setApplyRemainder(true)canvas.setGridGutter(0);canvas.setGridSize(1.3);*/try{  canvas.initCanvas();  $.writeln(canvas.getCanvas().layers.length);  var boo = [];  for (var j = 0; j < canvas.getCanvas().artLayers.length; j++)   {      //canvas.getCanvas().layers[j].name = "Photo-" + (j+1);      //$.writeln(canvas.getCanvas().layers[j].name);      //$.writeln(canvas.getCanvas().layers[j].bounds);      boo.push(canvas.getCanvas().layers[j].name);    }  boo.sort();   for (var j = 0; j < boo.length; j++)   {      //canvas.getCanvas().layers[j].name = "Photo-" + (j+1);      $.writeln(boo[j]);      //$.writeln(canvas.getCanvas().layers[j].bounds);    }}catch (e){   $.writeln(e.message);}renderer.setEnableNotifiers(false);