#include "../../Include.jsx";CSGlobal.setDebugLevel(1);try{  for (var d = 0; d < app.documents.length; d++)  {    CSGlobal.csDebug(1, "Number of open documents: " + app.documents.length);    app.activeDocument = app.documents[d];        // Create Fragment from open active Ps smob document, assumes one smart object layer in file    var f = new Fragment();    f.setFragmentDocument(app.documents[d]);    f.setKey(0);    f.setFragmentLayer(f.getFragmentDocument().artLayers[f.getKey()]);    f.setRandomRotation();    f.writeKeywords();        app.documents[d].save();  }    /*for (var d = 0; d < app.documents.length; d++)  {    app.documents[d].close();  }*/}catch (e){ CSGlobal.csDebug(1,e.message);}